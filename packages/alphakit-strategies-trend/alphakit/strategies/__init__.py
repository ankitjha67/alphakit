"""Strategy families.

Each family (trend, mean-reversion, carry, value, volatility, ...) lives
in its own sub-package under ``alphakit.strategies`` so that a user can
install only what they need via optional extras.
"""
